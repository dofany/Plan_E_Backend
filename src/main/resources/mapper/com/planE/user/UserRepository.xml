<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.planE.user.repository.UserRepository">
<!--
    <select id="getUserList" resultType="com.planE.user.dto.UserDto">
        select * from user;
    </select>
-->

    <!-- 전체 사용자 조회 -->

    <select id="userFindAll" resultType="com.planE.user.dto.UserDto">
        SELECT *
        FROM USER_BAS
    </select>



    <!-- 개별 사용자 조회 - 사용자 이름 검색-->
    <!--
    <select id="userFindOne" resultType="com.planE.user.dto.UserDto" parameterType="string">
        SELECT  USER_NM,
                NICK_NAME,
                EMAIL,
                PHONE_NUM,
                USER_BIRTH_DATE
            FROM USER_BAS
        <where>
            <if test="userNm != null and userNm != ''">
                AND user_nm = #{userNm}
            </if>
        </where>
    </select>
    -->

    <!-- 개별 사용자 조회 - 사용자 이메일 검색-->
    <select id="userFind" resultType="com.planE.user.dto.UserDto" parameterType="string">
        SELECT
            EMAIL,
            USER_NM,
            NICK_NAME,
            PHONE_NUM,
            USER_BIRTH_DATE
            FROM USER_BAS
        <where>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
        </where>
    </select>

</mapper>